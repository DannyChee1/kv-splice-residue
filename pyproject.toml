[project]
name = "scm"
version = "0.1.0"
description = "Speculative Context Materialization: measurement harness"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
